import * as strings from "../src/utils/strings";

// partially generated with chatGPT

describe("strings.ts", () => {
	it("should export allInstruments", () => { expect(strings.allInstruments).toBeDefined(); });
});
