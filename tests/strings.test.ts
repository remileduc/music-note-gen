import * as strings from "../src/utils/strings";

// partially generated with chatGPT

describe("strings.ts", () => {
	it("should export mi string", () => { expect(strings.miStringNotes).toBeDefined(); });

	it("should export la string", () => { expect(strings.laStringNotes).toBeDefined(); });

	it("should export re string", () => { expect(strings.reStringNotes).toBeDefined(); });

	it("should export sol string", () => { expect(strings.solStringNotes).toBeDefined(); });
});
