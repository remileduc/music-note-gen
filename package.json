{
  "name": "music-note-gen",
  "version": "0.1.0",
  "private": true,
  "description": "Générateur de notes de musique pour entrainement pour débutants aux instruments à cordes.",
  "keywords": [
    "musique",
    "violon",
    "guitare",
    "débutant"
  ],
  "homepage": "https://github.com/remileduc/music-note-gen",
  "author": "remileduc (https://github.com/remileduc)",
  "license": "MIT",
  "repository": "github:remileduc/music-note-gen",
  "type": "module",
  "scripts": {
    "dev": "npm run prebuild && next dev --experimental-https",
    "prebuild": "cp LICENSE public/ && marked --gfm --breaks -i README.md -o public/README.html",
    "build": "next build",
    "postbuild": "node src/list_out_dir.ts out/ out/files_to_cache.json",
    "start": "serve --no-clipboard --ssl-cert certificates/localhost.pem --ssl-key certificates/localhost-key.pem out/ || echo 'You need to run `npm run dev` at least once to create the certificates'",
    "lint": "eslint --cache --cache-location .next/cache/eslint/",
    "test": "jest",
    "test-ci": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "next": "^16.0.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "vexflow": "^5.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "babel-plugin-react-compiler": "^1.0.0",
    "eslint": "^9.39.1",
    "eslint-config-next": "^16.0.3",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "jest-junit": "^16.0.0",
    "marked": "^17.0.0",
    "serve": "^14.2.5",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
